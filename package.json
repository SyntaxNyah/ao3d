{
  "name": "ao3d",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AO3(D) — browser-based MMD character maker for Attorney Online 2",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@babylonjs/core": "9.18.1",
    "aolib-ts": "^1.2.0",
    "babylon-mmd": "^1.3.0"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "typescript": "^6.0.3",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
